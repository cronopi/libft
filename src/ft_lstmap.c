#include "../inc/libft.h"

t_list	*ft_lstmap(t_list *lst, void *(*f)(void *), void (*del)(void *))
{
/* 	t_list *str;
	int i;

	i = ft_lstsize(lst);
	str = malloc();
	while (lst != NULL)
	{
		str = ft_lstnew((*f)(lst->content));
		str
		lst = lst->next;
	}
	return (str); */
}
